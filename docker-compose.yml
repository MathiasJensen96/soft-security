services:
  api:
    build: .
    ports:
      - "81:80"
    env_file: .env
    networks:
      - app
      - db

  db:
    image: mysql:8.0.34
    volumes:
      - ./scripts:/docker-entrypoint-initdb.d
    env_file: .env
    networks:
      - db

networks:
  app:
    driver: bridge
  db:
    driver: bridge
